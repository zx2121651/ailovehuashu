## 2024-05-18 - Admin UI User Table Enhancement\n**Learning:** High-end modern tables need better interactive states (hover actions, row highlights, dynamic badges with fallback states) to improve admin experience. Standardizing the table UI across the admin dashboard is key to a cohesive UX.\n**Action:** Reused the Dashboard gradient styles and `animate-in` transitions to create a visually consistent and responsive `Users.jsx`. Addressed useEffect dependency warnings by moving `fetchUsers` into the effect scope.
## 2024-05-18 - Admin Dashboard Error Handling\n**Learning:** When modernizing UI components, do not discard existing error/loading logic. A visually appealing layout must also handle API failures gracefully. Missing error states lead to silent failures.\n**Action:** Reinstated and stylized the error state in Dashboard.jsx. Also fixed scoping for generic refactors (like useCallback for fetch data used in dependencies AND actions).
